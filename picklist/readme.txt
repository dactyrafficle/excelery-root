this bas file needs 2 things:

1. alookup2, which is my alookup function in exceleryroot/functions
2. getProductData, which is a fn that converts a range of data (from tacocat) and converts it to an array; alookup uses an array

so it feels like there are dependencies i guess
