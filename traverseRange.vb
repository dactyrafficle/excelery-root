'range.rows is a collection
'so range.rows.count returns a long, which represents the number of row objects in the rows collection

'you could do range.cells(x,y) or range.rows.cells(x,y) or range.columns.cells(x,y)
'tho u can also do range(i) but then you are counting cell by cell in the range

sub abc()

  

end sub
